1) Clone the repository

2) pip install -r requirements.txt ( one time )

3) Create your own .env file ( one time )

4) in your bash ( to clone the db)
psql -U their_username -c "DROP DATABASE student_forum_db;" (don't do if first time setup)
psql -U their_username -c "CREATE DATABASE student_forum_db;"
pg_restore -U their_username -d student_forum_db -F c db_backup.sql

Do this after every pull to completely clone the db I pushed ( will be necessary if I add new tables )
( If I don't add any new tables you can use - 'python manage.py migrate' instead )

5)python manage.py runserver
